# Server environment example
# Copy this file to .env and set real values

# Port your Express app listens on
PORT=8080

# Internal Mongo connection for Docker network
MONGODB_URI=mongodb://mongo:27017/notes

# Upstash rate limiter credentials (optional; enable rateLimiter only if set)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
